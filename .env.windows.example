# Windows Environment Configuration for Supertest

# ===== PROXY CONFIGURATION (for Corporate Networks) =====
# Set these if you're behind a corporate proxy that requires authentication

# HTTP Proxy (replace with your actual proxy details)
# Format: http://username:password@proxy.company.com:port
# HTTP_PROXY=http://your-username:your-password@proxy.company.com:8080
# HTTPS_PROXY=http://your-username:your-password@proxy.company.com:8080

# Alternative format without authentication (if proxy doesn't require auth)
# HTTP_PROXY=http://proxy.company.com:8080
# HTTPS_PROXY=http://proxy.company.com:8080

# Bypass proxy for local/internal networks
# Comma-separated list of hostnames, domains, or IP ranges
NO_PROXY=localhost,127.0.0.1,.local,192.168.0.0/16,10.0.0.0/8,company.internal,.company.com

# ===== PLAYWRIGHT CONFIGURATION =====
# Force Playwright to use local browser installation
PLAYWRIGHT_BROWSERS_PATH=0

# Skip browser download during npm install (if browsers are already installed)
# PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1

# ===== DATABASE CONFIGURATION =====
# PostgreSQL connection
DATABASE_URL=postgresql://postgres:password@localhost:5432/supertest

# Individual database connection settings (alternative to DATABASE_URL)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=password
DB_NAME=supertest

# ===== REDIS CONFIGURATION =====
# Redis connection for job queues
REDIS_URL=redis://localhost:6379
REDIS_HOST=localhost
REDIS_PORT=6379
# REDIS_PASSWORD=your-redis-password

# ===== S3/MINIO CONFIGURATION =====
# MinIO or S3 compatible storage
AWS_ACCESS_KEY_ID=minioadmin
AWS_SECRET_ACCESS_KEY=minioadmin
AWS_REGION=us-east-1
AWS_ENDPOINT=http://localhost:9000
S3_BUCKET_NAME=supertest-reports
S3_FORCE_PATH_STYLE=true

# ===== APPLICATION CONFIGURATION =====
# Execution timeouts (in milliseconds)
TEST_EXECUTION_TIMEOUT_MS=120000
JOB_EXECUTION_TIMEOUT_MS=900000

# Capacity limits
RUNNING_CAPACITY=5
QUEUED_CAPACITY=20

# Scheduler settings
DISABLE_JOB_SCHEDULER=false
DISABLE_MONITOR_SCHEDULER=false

# ===== WINDOWS-SPECIFIC SETTINGS =====
# Set NODE_ENV for development
NODE_ENV=development

# Ensure proper timezone handling
TZ=UTC

# Windows Terminal encoding (if needed)
# CHCP=65001

# ===== AUTHENTICATION =====
# Better Auth configuration
BETTER_AUTH_SECRET=your-secret-key-here-change-this-in-production
BETTER_AUTH_URL=http://localhost:3000

# Email configuration (for notifications)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
# SMTP_FROM=your-email@gmail.com

# ===== INSTRUCTIONS =====
# 1. Copy this file to .env.local or .env
# 2. Uncomment and fill in the proxy settings if you're in a corporate network
# 3. Update database and Redis connection details
# 4. Change authentication secrets
# 5. For Windows development, consider using WSL2 for better compatibility