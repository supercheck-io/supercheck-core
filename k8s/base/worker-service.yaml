apiVersion: v1
kind: Service
metadata:
  name: worker
  namespace: supercheck
  labels:
    app: worker
spec:
  type: ClusterIP
  ports:
  - port: 3001
    targetPort: 3001
    protocol: TCP
    name: http
  selector:
    app: worker 