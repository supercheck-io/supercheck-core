apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  namespace: supercheck
type: Opaque
stringData:
  # These are template values - replace with actual secrets in overlays or via external secret management
  DATABASE_URL: "postgres://postgres:password@postgres:5432/supercheck"
  AWS_ACCESS_KEY_ID: "minioadmin"
  AWS_SECRET_ACCESS_KEY: "minioadmin" 